{
  "name": "real_final_monorepo",
  "private": true,
  "workspaces": {
    "nohoist": [
      "**/frontend",
      "**/frontend/**"
    ],
    "packages": [
      "packages/*"
    ]
  },
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "backoffice": "yarn workspace backoffice",
    "upload-site": "yarn workspace upload-site",
    "backoffice-build": "yarn workspace backoffice build"
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "prettier": "^2.8.3",
    "typescript": "^4.9.4"
  }
}
